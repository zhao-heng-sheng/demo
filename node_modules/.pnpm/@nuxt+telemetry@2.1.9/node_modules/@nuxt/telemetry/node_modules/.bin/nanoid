#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/mnt/e/myProject/demo/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/mnt/e/myProject/demo/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../nanoid@4.0.1/node_modules/nanoid/bin/nanoid.js" "$@"
else
  exec node  "$basedir/../../../../../../nanoid@4.0.1/node_modules/nanoid/bin/nanoid.js" "$@"
fi
